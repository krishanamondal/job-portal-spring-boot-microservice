server:
  port: 5001

spring:
  application:
    name: job-portal-user-service
  datasource:
    url: jdbc:postgresql://localhost:5432/job-portal-user
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect